rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TejasOS MVP: Open access for Demo/Kiosk mode
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
